name: Deploy to dev
on: workflow_dispatch

env:
  DOCKER_REGISTRY: ghcr.io
  IMAGE_NAME: kartverket/kommuneinfo

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      actions: read

    outputs:
      version: ${{ steps.set_output.outputs.version }}

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Login to Github Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build docker image and push
        id: build
        uses: docker/build-push-action@v4
        with:
          context: .
          tags: ${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}
          push: true

      - name: Summarize Image Digest and Set Output
        id: set_output
        run: |
          echo "Full Image URL: ${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}@${{steps.build.outputs.digest}}" >> $GITHUB_STEP_SUMMARY
          echo "version=${{ github.ref_name }}@${{steps.build.outputs.digest}}" >> $GITHUB_OUTPUT

  deploy-argo:
    needs: build
    uses: kartverket/smia-apps/.github/workflows/release-app.yml@main
    with:
      application: kommuneinfo
      version: ${{ needs.build.outputs.version }}
      namespace: "smia-tjenester-main"
      deploy_dev: true
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_ARGOCD }}
